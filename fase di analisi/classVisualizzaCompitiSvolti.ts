 class visualizzaCompitiSvolti {
    compito: string[];

    constructor(compito: string[]) {
        //inserisco nel vettore solo i compiti terminati
         this.compito = compito;
    }

    strainer(filtro: string){
        //visualizza solo i compiti svolti con un determinato filtro
    }
 }